# Система анализа текстов и токенизации

Комплексная система для анализа русскоязычных текстов, включающая парсинг новостей, предобработку текстов, анализ методов токенизации и обучение подсловных моделей.

## 📋 Этапы проекта

### 3.1. Этап 1. Формирование экспериментального корпуса текстов
**Задача**: Составить репрезентативный корпус современных русскоязычных новостных текстов.

**Источники данных**:
- kommersant.ru - Коммерсантъ (основной источник)

**Структура данных** (обязательно для каждой статьи):
- Заголовок
- Основной текст
- Дата публикации
- URL-адрес
- Категория/рубрика (при наличии)

**Требования к корпусу**:
- Общий объём: не менее 50 000 слов
- Формат хранения: JSONL (каждая строка — отдельный JSON-объект со статьёй)

### 3.2. Этап 2. Предварительная обработка и очистка текста
**Модуль**: `text_cleaner.py`

**Функциональность**:
- Удаление HTML-разметки, служебных символов, рекламных блоков
- Стандартизация пробельных символов
- Приведение текста к нижнему регистру (опционально)
- Фильтрация стоп-слов (nltk.corpus.stopwords)

### 3.3. Этап 3. Проектирование универсального модуля предобработки
**Модуль**: `universal_preprocessor.py`

**Функциональность**:
- Стандартизация пунктуации и пробелов
- Замена числительных, URL-адресов и email на унифицированные токены: `<NUM>`, `<URL>`, `<EMAIL>`
- Обработка общеязыковых и специальных сокращений (например: "т.е." → "то есть", "г." → "год")

### 3.4. Этап 4. Сравнительный анализ методов токенизации и нормализации
**Модуль**: `tokenization_analysis.py`

**Методы**:
- **Токенизация**: Наивная, регулярные выражения, NLTK, spaCy, razdel
- **Стемминг**: PorterStemmer, SnowballStemmer (русский)
- **Лемматизация**: pymorphy2, spaCy (ru_core_news_sm)

**Метрики оценки**:
- Объём словаря — количество уникальных токенов
- Доля OOV (Out-of-Vocabulary) — % слов, не вошедших в словарь
- Скорость обработки — время на 1000 статей
- Семантическая согласованность — сохранение смысла

### 3.5. Этап 5. Обучение подсловных моделей токенизации
**Модуль**: `subword_models.py`

**Модели**:
- Byte Pair Encoding (BPE)
- WordPiece
- Unigram Language Model

**Параметры обучения**:
- Размер словаря: 8 000 – 32 000 токенов
- Минимальная частота токена: 2–5

**Метрики оценки**:
- Процент фрагментации слов — доля слов, разбитых на 2+ подслова
- Коэффициент сжатия — отношение числа исходных слов к числу токенов
- Эффективность реконструкции — точность восстановления исходного текста

### 3.6. Этап 6. Разработка веб-интерфейса для интерактивного анализа
**Модуль**: `web_interface.py`

**Функциональные требования**:
- Загрузка датасетов (свой файл или предзагруженный)
- Выбор языка и метода токенизации/нормализации
- Генерация динамического отчёта с графиками:
  - Распределение длин токенов
  - Частотность токенов
  - Доля OOV
- Отображение отчёта на главной странице + возможность экспорта в HTML/PDF

### 3.7. Этап 7. Публикация моделей в Hugging Face Hub
**Требования к оформлению**:
- Model Card для каждой модели
- Описание корпуса, параметров, метрик
- Пример использования

## 🚀 Возможности

### 📰 Парсинг новостей
- **Коммерсантъ**: Основной источник новостей с парсингом статей по ID
- **Перебор номеров**: Автоматический парсинг статей с перебором номеров (например: https://www.kommersant.ru/doc/8059662)
- **Множественные категории**: Политика, экономика, общество, мир, спорт, культура, бизнес, финансы
- **Масштабируемость**: Настраиваемое количество статей и диапазон ID для анализа
- **Надежность**: Обработка ошибок и задержки между запросами

### 🔧 Предобработка текстов
- **Очистка HTML**: Удаление разметки и декодирование HTML сущностей
- **Нормализация**: Стандартизация пунктуации, кавычек, тире и пробелов
- **Замена токенов**: Числа, URL, email, телефоны, даты, время, валюты
- **Расшифровка сокращений**: Автоматическая расшифровка русских сокращений

### ✂️ Анализ токенизации
- **Множественные методы**: Наивная, regex, NLTK, spaCy, razdel
- **Нормализация**: Porter/Snowball стемминг, spaCy/pymorphy2 лемматизация
- **Метрики оценки**: OOV rate, семантическое сходство, скорость обработки
- **Сравнительный анализ**: Детальное сравнение эффективности методов

### 🧩 Подсловные модели
- **BPE (Byte Pair Encoding)**: Эффективная токенизация для больших корпусов
- **WordPiece**: Модель, используемая в BERT
- **Unigram Language Model**: Вероятностная модель токенизации
- **SentencePiece**: Универсальная библиотека для подсловной токенизации

### 🌐 Веб-интерфейс
- **Интерактивный анализ**: Настройка параметров через веб-интерфейс
- **Визуализация результатов**: Графики и таблицы для анализа
- **Экспорт данных**: Сохранение результатов в различных форматах
- **Реальное время**: Мгновенная обработка и отображение результатов

## 📦 Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd NLP
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Дополнительная настройка

#### spaCy модель для русского языка:
```bash
python -m spacy download ru_core_news_sm
```

#### NLTK данные:
```python
import nltk
nltk.download('punkt')
nltk.download('stopwords')
```

## 🚀 Использование

### Командная строка

#### Полный пайплайн анализа:
```bash
python nlp.py
# Выберите опцию 1 для полного пайплайна
```

#### Отдельные компоненты:
```bash
# Парсинг новостей
python kommersant_parser.py

# Анализ токенизации
python tokenization_analysis.py

# Обучение подсловных моделей
python subword_models.py
```

### Веб-интерфейс

```bash
streamlit run web_interface.py
```

Откройте браузер по адресу `http://localhost:8501`

### Программный интерфейс

```python
from nlp import NLPAnalysisPipeline

# Создание пайплайна
pipeline = NLPAnalysisPipeline(language='russian')

# Полный анализ с универсальным парсером
results = pipeline.run_full_pipeline(max_articles=100, use_universal=True)

# Отдельные шаги
articles = pipeline.collect_news_corpus(50, use_universal=True)
processed = pipeline.preprocess_corpus()
analysis = pipeline.analyze_tokenization()

# Использование парсера Коммерсанта
from kommersant_parser import KommersantParser
from universal_news_parser import UniversalNewsParser

# Основной парсер Коммерсанта
kommersant_parser = KommersantParser()
articles = kommersant_parser.parse_article_range(8050000, 8059999, max_articles=100)

# Универсальный парсер (настроен на Коммерсант)
universal_parser = UniversalNewsParser()
articles = universal_parser.parse_site('kommersant.ru', max_articles_per_category=10)
```

## 📊 Структура проекта

```
NLP/
├── kommersant_parser.py       # Основной парсер Коммерсанта
├── universal_news_parser.py   # Универсальный парсер (настроен на Коммерсант)
├── text_cleaner.py            # Очистка текстов (Этап 2)
├── universal_preprocessor.py  # Универсальная предобработка (Этап 3)
├── tokenization_analysis.py   # Анализ токенизации (Этап 4)
├── subword_models.py          # Подсловные модели (Этап 5)
├── web_interface.py           # Веб-интерфейс (Этап 6)
├── nlp.py                     # Основной модуль пайплайна
├── requirements.txt           # Зависимости
└── README.md                  # Документация
```

## 📈 Результаты анализа

### Метрики токенизации:
- **OOV Rate**: Доля слов вне словаря
- **Семантическое сходство**: Сохранение смысла при обработке
- **Скорость обработки**: Время на обработку текста
- **Размер словаря**: Количество уникальных токенов

### Метрики подсловных моделей:
- **Коэффициент фрагментации**: Доля слов, разбитых на подслова
- **Коэффициент сжатия**: Отношение исходных слов к токенам
- **Эффективность реконструкции**: Точность восстановления текста

## 🔧 Конфигурация

### Настройки предобработки:
```python
from universal_preprocessor import PreprocessingConfig

config = PreprocessingConfig(
    replace_numbers=True,
    replace_urls=True,
    replace_emails=True,
    normalize_punctuation=True,
    expand_abbreviations=True,
    to_lowercase=False
)
```

### Настройки парсера:
```python
from kommersant_parser import KommersantParser

parser = KommersantParser(delay=1.0)  # Задержка между запросами
```

## 📝 Примеры использования

### 1. Быстрый анализ текста
```python
from text_cleaner import TextCleaner
from universal_preprocessor import UniversalPreprocessor

# Очистка текста
cleaner = TextCleaner()
clean_text = cleaner.clean_text("Ваш текст здесь...")

# Предобработка
preprocessor = UniversalPreprocessor()
processed_text = preprocessor.preprocess_text(clean_text)
```

### 2. Анализ токенизации
```python
from tokenization_analysis import TokenizationAnalyzer

analyzer = TokenizationAnalyzer()
results = analyzer.analyze_corpus(texts)
```

### 3. Обучение подсловных моделей
```python
from subword_models import SubwordModelTrainer

trainer = SubwordModelTrainer()
results = trainer.train_all_models(texts, vocab_sizes=[8000, 16000])
```

## 🐛 Устранение неполадок

### Ошибки импорта:
```bash
# Установка недостающих зависимостей
pip install -r requirements.txt

# Обновление pip
pip install --upgrade pip
```

### Ошибки spaCy:
```bash
# Переустановка модели
python -m spacy download ru_core_news_sm --force
```

### Ошибки NLTK:
```python
import nltk
nltk.download('punkt')
nltk.download('stopwords')
```

## 📄 Лицензия

Проект распространяется под лицензией MIT.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории или обратитесь к разработчикам.

---

**Автор**: Система анализа текстов и токенизации  
**Версия**: 1.0.0  
**Дата**: 2024
